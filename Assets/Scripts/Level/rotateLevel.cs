using UnityEngine;
using System.Collections;

public class rotateLevel : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        //transform.Rotate(this.transform.position, 0);
	}
}
